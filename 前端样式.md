
# 抖音 Web 端 (2025-2026) 开发样式参考指南

## 1. 核心设计原子 (Design Tokens)

### 1.1 色彩系统 (CSS Variables)

```css
:root {
  /* 背景层级 */
  --dy-bg-body: #121212;      /* 全局底色 */
  --dy-bg-container: #161618; /* 容器/侧边栏底色 */
  --dy-bg-elevated: #252526;  /* 悬浮层/模态框 */
  --dy-bg-hover: #2D2D2D;     /* 交互悬停态 */
  
  /* 品牌与功能色 */
  --dy-brand-red: #FE2C55;    /* 核心 CTA (关注、发送) */
  --dy-brand-cyan: #25F4EE;   /* 辅助/故障风艺术色 */
  
  /* 文字颜色 (基于不透明度) */
  --dy-text-primary: rgba(255, 255, 255, 1);    /* 一级标题 */
  --dy-text-secondary: rgba(255, 255, 255, 0.88);/* 正文、评论 */
  --dy-text-tertiary: rgba(255, 255, 255, 0.55); /* 时间、地点、次要信息 */
  
  /* 辅助线 */
  --dy-border-default: 1px solid rgba(255, 255, 255, 0.08);
}

```

### 1.2 字体排印 (Typography)

* **基础栈：** `"PingFang SC", "HarmonyOS Sans", "Microsoft YaHei", sans-serif`
* **数据专用 (关键点)：** 凡是涉及数字（点赞数、热度、价格、时长），必须使用 **`DIN Condensed`** 或 **`DIN Alternate`**。
* **比例规则：** 采用 4px 步进系统（12, 14, 16, 20, 24px）。行高统一为 `1.5`。

---

## 2. 响应式布局架构 (Grid System)

抖音 Web 端采用基于视口宽度的“三栏动态重构”：

| 视口宽度 | 侧边导航 (Left) | 视频区域 (Center) | 交互侧栏 (Right) |
| --- | --- | --- | --- |
| **< 1024px** | 64px (图标模式) | 居中填满 | 浮层/抽屉模式 |
| **1024-1440px** | 240px (展开模式) | 居中自适应 | 336px 固定宽度 |
| **> 1440px** | 240px | 比例拉伸 (Max 60%) | 360px 常驻显示 |

---

## 3. 核心组件开发规范

### 3.1 全局导航栏 (Header)

* **高度：** `60px`。
* **视觉效果：** `backdrop-filter: blur(20px); background: rgba(22, 22, 24, 0.8);`。
* **搜索框：** 胶囊形（Pill Shape），圆角 `20px`，高度 `40px`。

### 3.2 视频播放器 (XGPlayer Skin)

* **背景填充：** 对于 9:16 的竖屏视频，左右两侧需使用 `canvas` 或 `filter: blur(30px)` 实现视频内容的实时高斯模糊背景。
* **进度条交互：**
* 默认高度：`2px`。
* Hover 态：高度变为 `4px`，弹出 `Thumbnail Preview`（缩略图预览）。


* **沉浸模式：** 定义 `.is-immersive` 类，隐藏除 `.video-content` 以外的所有 DOM 节点。

### 3.3 交互侧边栏 (Drawer)

* **评论区逻辑：** 必须使用**虚拟列表 (Virtual List)**。
* **缩进线：** 二级回复左侧需有 `1px solid --dy-bg-hover` 的引导线。
* **圆角：** 大容器 `12px`，小按钮 `4px` 或 `8px`。

---

## 4. 交互动效指南 (Framer Motion / GSAP)

* **点击反馈：** 点赞、关注按钮点击时需有 `scale: [1, 1.2, 1]` 的弹簧效果。
* **加载状态：** 使用深灰色骨架屏（Skeleton），配合 `linear-gradient` 的闪烁动画。
* **层级切换：** 弹窗出现时，背景背景遮罩使用 `rgba(0,0,0,0.6)`。

---



**如果你想让 AI 写一个视频卡片组件，可以这样输入：**

> "请参照抖音 2025 Web 端规范编写一个 React/Vue 组件。
> 1. 背景使用 `--dy-bg-container`。
> 2. 视频封面需具备 `16:9` 比例，左下角展示 `DIN` 字体的播放量，右下角展示时长。
> 3. Hover 时，封面需放大 1.05 倍并自动预览视频。
> 4. 标题使用 `--dy-text-primary`，两行截断。
> 5. 间距遵循 4px 栅格（使用 gap-4 等）。"
> 
> 

---

## 6. 技术栈实施建议

* **框架：** Vue 3 (Script Setup) 或 React 18+。
* **样式：** Tailwind CSS (推荐，因其高度契合原子化设计)。
* **图标：** 采用 SVG Sprite，默认线性描边，Active 状态切换为实心。
* **状态管理：** 必须具备全局 `isMuted` (静音状态) 和 `currentVideoId` 的同步逻辑。
